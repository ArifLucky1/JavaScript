// forEach() executes a function for each element.
//  It does not return a new array.


    const nums = [1, 2, 3, 4]

    nums.forEach(n => console.log(n));

    
// ** Use for side effects (like Logging, API calls, etc.) **