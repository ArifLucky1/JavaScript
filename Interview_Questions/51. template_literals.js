// Template literals allow embedded expressions and multi-line strings using backticks (``)

        const name = "Arif";
        const age = 25;
        const msg = `Hi ${name}, you are ${age} years old.`;

        console.log(msg);               // Hi Arif, you are 25 years old.
        
    //  *** Use `${}` to embed expressions. ***